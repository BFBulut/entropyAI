# Faz 13 İlerleme Raporu — 13-A2 (v0.10.2): v0.10.1 geri bildiriminin kapanışı

Tarih: 2026-09-10 · Branch `ai/v0.1.7` · Taban `v0.10.1` · Etiket `v0.10.2` · Build: `dist\EntropyAI\EntropyAI.exe`

Plan: `2026-09-10_Faz13_Plan_ve_Yol_Haritasi.md` §6. Kullanıcı v0.10.1'i gerçek ekranda denedi (8 görsel), yedi bulgu bildirdi ve üç kararı onayladı (13-B taşıma, 13-C Desk zinciri ~60k, wiki ikinci parti ~30k). 13-B tek başına koşacağı için bu yedi bulgu önce ayrı dilimde kapatıldı.

## Bulgu → kök neden → sonuç
| # | Kullanıcının gördüğü | Kök neden (kanıt) | Sonuç |
|---|---|---|---|
| 1 | Yetenekler'de "Etkin" kutusu donduruyor ("EntropyAI.exe is not responding") | Toggle iki kez tam yeniden kurulum tetikliyordu (doğrudan `refresh_skills` + `skills_updated` sinyali; 22 `SKILL.md` ayrıştırma 232 ms + yetenek başına kasa taraması 1.058 ms) ve aynı sinyal bütün hafızayı okuyan telemetriyi (152 ms) çağırıyordu | toggle yalnız durumu yazar, rozeti yerinde çevirir; katalog önbelleği, sinyal bastırma, 300 ms birleştirme: **1.961 → 2,4 ms**; kapı Yetenekler ekranını da ölçüyor (152 → 6 ms) |
| 2 | Bazı düğmeler hâlâ boş kare | `QPushButton("")` kuruluyor, `setIcon` hiç çağrılmıyordu (yetenek satırları 5, ajan kartı 4–6, graf 1); G13-1 kapısı `accessibleName` dolu diye susuyordu | ikon + erişilebilir ad + çizilemezse metin yedeği; kapı 7 ekranı tarıyor, ikonu piksel ölçüyor, ad kapıyı susturmuyor: **8 → 0** |
| 3 | "Raporu açSohbete al" bitişik | Qt zengin metin satır içi öğelerde `margin`i yok sayıyor | eylemler ayrı satırda tablo hücreleri; `toPlainText` → "Raporu aç\nSohbete al" |
| 4 | Araştırmacı çalışırken kart "2 sa önce" diyor; çekirdek mora geçiyor | rozet oturum dosyasının yaşını ve en taze *herhangi* sağlayıcının oturumunu okuyordu; çekirdek moru `chat_mode.py`'de arka plan görevine bağlanmış üç satırdan geliyordu (köprüler `core_state_changed` yaymıyor) | `AgentSessionStore.status()` sözleşmesi (`state.json`, güncel sağlayıcı, açılış uzlaştırması); rozet "çalışıyor · 1 dk 12 sn" / "son koşu: az önce"; gezinmede "Ajanlar ●1"; çekirdek yalnız Entropy'nin turunu yansıtır (`ARCHITECTURE.md` §8) |
| 5 | Görevler kalabalık | QA canlı koşularının 9 kartı projeksiyonda asılıydı (dosyaları zaten yok, "ayrışma 11"); arka plan görevleri her zaman açık; önizleme uzun | 9 QA kartı kanıtla arşivlendi (`Entropy/_archive/2026-09-10/qa-cards/`, FSM T13 `canceled` + gerekçe; kullanıcının 3 canivopets kartı ve ofis kartı dokunulmadı), ayrışma 11 → 2; arka plan görevleri varsayılan katlı; önizleme ≤ 3 satır |
| 6 | Bir görev koşarken ~20 pencere açılıp kapanıyor | `setParent(None)` widget'ı üst düzey pencereye çevirir; `task_board_widget.refresh_cards` her kartı böyle söküyordu (20 kart = 20 parlama; deneyle 5/5 kanıtlandı); ayrıca durdurma yolundaki `taskkill`/`git` alt süreçleri konsol açıyordu | `ui/widgets/lifecycle.py` (12 çağrı), kalıcı kapı `orphan_reparents = 0`; `platform/proc.popen_kwargs()` (25 alt süreç çağrısından 19'u gizli, 5'i bilerek görünür açıcı); kart koşusunun kendisi 1 gizli süreç |
| 7 | "Araştır" deyince her şey beyinden çekildi; yanıt kimlik düğümü | 12-F'nin `kind=research` kısa devresi güven 0,49 ≥ 0,40 ile tetiklendi; `brain_has_answer` kimlik düğümünü yanıt saydı; Entropy kendisi ikinci kartı `write` yaparak aşmıştı | otomatik kısa devre **varsayılan kapalı**; yalnız açık `--brain-only` + güven ≥ 0,75 + kaynaklı + kimlik/legacy dışı; tazelik ipucu varsa asla; beyin ajana `[BEYİN]` bağlamı; beyin paketinden kimlik bloğu çıktı (sorgu başına ~219 token; K6 %40 → %43) |
| + | Üst çubuk "29 Claude ✓ ma…" kırpık; "Oturumu yenile" sohbete otonom görev kartı düşürüyor | rozetlerde 76 px sabit tavan; oturum yenileme otonom görev sonucu kanalını kullanıyordu | genişlik yazı tipinden; oturum yenileme sessiz durum satırı (rapor/ledger yok) |

## Kapanış QA (v0.10.2)
- Tam süit **2.596 test, 0 hata** (613 s); spec eşlemesi (üç yeni modül) ve sürüm tek kaynak 0.10.2; `ui_audit --gate --final` exit 0 (`orphan_reparents` 0, `unnamed_icon_buttons` 0, `screens_swept` 7, `empty_interactive_count` 0, `click_latency_ms` 10).
- Build exit 0 (328 s), `--version` → `Entropy AI 0.10.2`, üç yeni modül pakette; günlükte `Traceback`/`CRITICAL` 0, yeni CrashDump yok. Kullanıcının uygulaması QA sırasında açık olduğu için build `dist_check`e alındı, kapanınca `dist`e aynalandı.
- Kapanışta dört küçük düzeltme (+15 test): kartta `brain_only` alanı (`/task --brain-only`, `[PANO board_create]`); Desk projeler panelindeki son konsol çağrısı gizli; `board.drift` olayları tekil `seq` ve kendi korelasyonuyla (kullanıcının `events.jsonl`'inde `seq` 41/45/46 ikişer kez vardı, dördü ilgisiz kartın zincirindeydi). Üç eski test sahte `brain_lookup` imzasıyla `TypeError`'ı sessizce yutuyordu; sözleşmeye uyduruldu ve imza testi eklendi.
- **Gerçek ekran (LG %200), 7/7:** "Etkin" tıklaması azami 3,6 ms; ikonlar 119/119 çizildi (kaynak ağacı); "Raporu aç | Sohbete al" ayrı; "çalışıyor · 1 dk 12 sn" rozeti ve "Ajanlar ●1"; üst çubuk metni %100 ve %200'de tam; QA kartları yok; **canlı kart koşusu boyunca 300 sn / 6.839 örnekte yeni konsol 0, üst düzey pencere 0** (canlı kart: tavan 20k, `board.stop` ile iptal, ledger satırı öksüz `RUNNING`, token NULL).
- Yalıtım: `skills_state.json` değişmedi, kasa Reports 423 → 423; marka 0/0.

## Kalanlar
- **R-13A2-1 (kullanıcı verisi, açık):** üç canivopets kartından ikisinin `.md` dosyası kasada yok (`…194435-canivopets-com-medya-uzm`, `…224237-canivopets-com-s-f-rdan-`); arşiv ya da `canceled` olayı yok, kasa genelinde kopya yok. QA kartı arşivleme adımı dosya taşımadı (dosyalar o adımdan önce de yoktu). Neden belirsiz: kullanıcı arayüzden sildiyse "Sil" yolunun olay yazmadan dosya silmesi ürün hatasıdır (13-C'de kapatılacak: silme = arşiv + olay). Kartlar olaylardan yeniden kurulabilirdi; kullanıcı 2026-09-11'de "silinmiş kalsın" dedi, geri kurulmadı. 13-C'de silme yolu arşiv + olay olacak.
- Paketlenmiş exe penceresinden ikon örneklemesi alınamadı (tek örnek kilidi; kullanıcının uygulaması açıktı) — ikonlar kaynak ağacından doğrulandı; exe'de yedek harf görünürse kusur.
- `ui_audit` kapanışında `EntropyEventBus already deleted` yıkım sırası hatası (`knowledge_graph.py` → `event_bus.py`); kapıyı etkilemiyor, 13-D.
- Görevler'de dar pencerede detay paneli asgarisinden geniş açılıp panoyu yatay kaydırıyor; önizleme son satırı üç nokta yerine kesiyor (13-D).
- 13-B `memory → brain` (onaylı, tek başına) → 13-C + wiki ikinci parti (onaylı) → 13-D.
